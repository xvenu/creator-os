# zoza-factory runtime + test dependencies.
# Production rendering is simulated with stdlib (no ffmpeg/PIL required);
# real providers plug in behind the provider registry without new deps.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
httpx>=0.27
python-dateutil>=2.8
pytest>=8.0
pytest-asyncio>=0.23
